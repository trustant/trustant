image/bin
image/trustable.json
image/opencode.md
workspace
workbench
tmp/
node_modules/
test-results/
playwright-report/
blob-report/
benchmark-results/
.env*
.key
.ops/
trustable-app
.DS_Store
.vscode/
appo/
out.json
image/Dockerfile.*
trustable-doctor.txt
opencode.json
_build.txt
Trustable
/dist/
/opencode.sh
.ghtoken
.1pw
# A 1Password service token for the TrustableLicenses vault can still land here
# when trulicense (now in trustable-installer) is run from this directory.
# Never commit it.
.op.json
*.lic

# screenshot.sh copies the launched app's animation here so it can be previewed
# in the editor. It is a scratch copy of <app>/screenshot.png, which is the
# versioned artifact — this one is never committed.
/screenshot.png
