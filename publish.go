package main

// Publish endpoints (POST /api/publish/push, /api/publish/local, /api/publish/remote)
// are stubs for now. The frontend publish dropdown shows "To be implemented" without
// calling any API.
