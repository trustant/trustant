Do not traverse up of the current directory to search for extra code.
This is a self contained serverless application,
there is NOT a server, the serverless functions are deployed to the serverless environment,
the frontend is built and published statically.

